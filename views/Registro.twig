<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Registro de Usuario</title>
    <style>
      body {
            font-family: Arial, sans-serif;
            background-color: #086B40; /* Fondo verde para la página */
        }
        .imagen-estilo {
    width: 200px;
    height: 250px;
    position: fixed; 
    bottom: 10px; 
    right: 10px; 
    z-index: 1; /* Asegura que la imagen esté por encima del fondo */
}
        .container {
            width: 300px;
            margin: 0 auto;
            padding: 20px;
            background-color: white; /* Fondo blanco para el contenedor */
            border-radius: 10px; /* Bordes redondeados para el contenedor */
        }
        input[type=text], input[type=email], input[type=password] {
            width: 100%;
            padding: 10px;
            margin: 5px 0 20px 0;
            display: inline-block;
            border: 1px solid #ccc;
            box-sizing: border-box;
        }
        button {
            background-color: #4CAF50;
            color: white;
            padding: 14px 20px;
            margin: 8px 0;
            border: none;
            cursor: pointer;
            width: 100%;
        }
        button:hover {
            opacity: 0.8;
        }
        .login-message {
            text-align: center;
            margin-top: 10px;
        }
    </style>
</head>
<body>

<div class="container">
    <h2>Formulario de Registro</h2>
    <p class="login-message">Si ya tienes una cuenta, <a href="/Login">entra aquí</a>.</p>
    <form id="registrationForm" action="/crearUsuario" method="post">
        <label for="fullname">Nombre Completo:</label>
        <input type="text" id="fullname" name="nombre" required>

        <label for="email">Correo Electrónico:</label>
        <input type="email" id="email" name="correo_electronico" required>

        <label for="password">Contraseña:</label>
        <input type="password" id="password" name="contrasena" required>
<label>
            <input type="checkbox" id="termsCheckbox" name="terms" required>
            Acepto los <a href="terminos_y_condiciones.html">términos y condiciones</a>
        </label>
        <input type="hidden" id="role_id" name="role_id" value="1">
        <button type="submit" id="submitButton">Registrarse</button>
    </form>
    <img src="https://induccionuabc.files.wordpress.com/2019/08/listas_grupos_curso_induccion.png?w=353&h=491" class="imagen-estilo">
                <svg viewBox="0 0 320 300">
</div>

<script>
    document.getElementById('registrationForm').onsubmit = function() {
        var checkbox = document.getElementById('termsCheckbox');
        if (!checkbox.checked) {
            alert('Debes aceptar los términos y condiciones para registrarte.');
            return false; // Cancela el envío del formulario
        } else {
            // Si todo está correcto, redirecciona a la página de donación
            window.location.href = "/login";
            return true; // Esto previene que el formulario se envíe de la manera tradicional
        }
    };
</script>


</body>
</html>